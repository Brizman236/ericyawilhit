body, article {
    font-family: 'Inter', sans-serif;
}

@font-face {
    font-family: 'Google Sans Code';
    src: url('/fonts/GoogleSansCode-Regular.ttf') format('truetype');
}

code, pre {
    font-family: 'Google Sans Code', monospace;
    font-size: 19px;
}

/* Conteneur du bloc */
pre {
    background: #0d1117; /* fond dark élégant */
    color: #e6edf3;
    padding: 16px;
    border-radius: 12px;
    overflow-x: auto;
    border: 1px solid #222b3a;
    box-shadow: 0 6px 18px rgba(0,0,0,0.25);
    line-height: 1.5;
}

/* Code inline */
code {
    background: rgba(110,118,129,0.15);
    padding: 2px 6px;
    border-radius: 6px;
}

/* Supprime double fond dans pre > code */
pre code {
    background: transparent;
    padding: 0;
    color: inherit;
}
